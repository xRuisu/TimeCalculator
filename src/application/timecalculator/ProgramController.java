package application.timecalculator;

public abstract class ProgramController {
	//Simple program controller class, can be expanded on. 
	//Handled other methods like killProgram(), and checks but wern't necessary so were removed.
	abstract void onEnable();
	abstract void onDisable();
}
